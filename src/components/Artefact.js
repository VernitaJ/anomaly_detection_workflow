import React from "react";
import artefact from "../arrows/artefact.svg";

const Artefact = () => {
  return (
    <div>
        <img
        src={artefact}
        alt="Anomaly Detection Process"
        style={{ width: "100%" }}
        />
    </div>
  );
};

export default Artefact;
